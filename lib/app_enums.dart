enum SplashScreenState { loading, error, updateAvailable, accessDenied }

enum LoginScreenState {
  mobileNumber,
  checkingMobileNumber,
  notRegistered,
  sendingSMS,
  otpScreen,
  verified,
  verificationFailed
}
